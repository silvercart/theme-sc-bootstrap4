module.exports = {
   SASS_SRC:  "client/scss", // Relative path to the sass files. Should be fine for most cases.
   CSS_DIR:   "client/css", // Relative path to the css files. Should be fine for most cases.
   JS_DIR:    "client/js", // Relative path to the js files. Should be fine for most cases.
   APP_URL:   "http://my.dev.domain.tld", // URL to the running application. Needed if you want to use BrowserSync.
   SYNC_PATH: "/path/to/my/project/themes/sc-bootstrap4", // Absolute path to the running application's theme directory (on server). Needed if your environment doesn't do that automatically.
};